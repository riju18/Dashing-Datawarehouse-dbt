dashing_warehouse:
  outputs:

    dev:
      type: postgres
      threads: 1
      host: localhost
      port: 5432
      user:
      pass:
      dbname: dvdrental
      schema: dashingdvdrental

    prod:
      type: postgres
      threads: [1 or more]
      host: [host]
      port: [port]
      user: [prod_username]
      pass: [prod_password]
      dbname: [dbname]
      schema: [prod_schema]

  target: dev
